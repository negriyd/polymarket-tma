# Copy → .env.local and fill in (pnpm dev reads .env*)
# Backend secrets (PRIVY_APP_SECRET etc.) belong in deploy/.env only — NEVER prefix with VITE_.

VITE_API_BASE_URL=http://localhost:8080
VITE_PRIVY_APP_ID=
VITE_POLYGON_RPC_URL=https://polygon-rpc.com
